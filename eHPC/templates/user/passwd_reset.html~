{% extends 'base.html' %}
{% block title %}
{{ _('reset password') }}
{% endblock %}

{% block content %}

<div class="container">
    <form class="form-horizontal panel-body password-reset" method="post">
        <h2 class="text-center" style="margin-bottom:1em"> {{ _('Reset Password') }} </h2>

		<div class="form-group ">
			<label for="email"> {{ _('Please enter your email') }} </label>
            <input class="form-control" data-widget-cid="widget-3" id="email" name="email" placeholder=example@gmail.com type="text">
            {% if message_email %}
            <div class="messages col-sm-10"> <p class="text-danger"> {{ message_email }} </p> </div>
            {% endif %}
        </div>

        <div class="form-group">
            <button class="btn btn-default btn-success">
                {{ _('submit') }}
            </button>
        </div>

    </form>
</div>
{% endblock %}

